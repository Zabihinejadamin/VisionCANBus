# VCU CAN Tool Requirements
# Install with: pip install -r requirements.txt

# Core CAN communication
python-can>=4.0.0
cantools>=37.0.0
typing-extensions>=4.0.0

# GUI
PyQt5>=5.15.0

# tkinter is usually built-in with Python
# If tkinter is not available, install with: apt-get install python3-tk (Linux)
